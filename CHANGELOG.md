# 更新日志

本文件记录 MiniMax H3 WebUI 的所有重要变更。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)，
版本号遵循 [Semantic Versioning](https://semver.org/lang/zh-CN/)。

## [Unreleased]

## [1.5.0] - 2026-08-28

### 新增
- **文件管理弹窗预览**：点击文件卡片弹出预览窗口，支持图片/音频/视频/文本四种类型
- **一键添加到槽位**：预览弹窗中直接将素材添加到指定参考槽位（图片 9 槽位、音视频各 3 槽位）
- **文本添加到提示词**：文本文件可直接追加到提示词输入框
- **视频槽位缩略图**：视频参考槽位自动显示封面预览
- **音频裁剪功能**：基于 AudioCrop 节点 (id 142)，支持 M:SS 格式的时间裁剪，默认截取前 3 秒
- **视频预置系统**：新增 `/api/presets/videos` 接口和 `loadVideoPresets()` 前端逻辑
- **默认预置素材**：卡通玩偶参考图、广告示范视频、配套提示词模板

### 改进
- **优雅 OOM 提示**：提取 GPU 显存占用信息（当前占用/设备限制/还需申请），以 ①②③④ 编号给出优化建议
- **原始错误折叠**：错误详情使用 `<details>` 折叠，默认隐藏避免占用界面空间
- **视频时长步进**：改为 1 秒步进（原 0.5 秒）
- **界面精简**：移除生成按钮下方空白行（`submitStatusText`）和冗余的视频播放器/下载按钮（`resultBox`）
- **槽位操作闭包化**：文件管理添加槽位操作改用闭包捕获 item，避免依赖全局变量

### 修复
- **素材去重**：上传文件采用内容 MD5 哈希命名（替代 UUID），避免重复素材覆盖和磁盘浪费
- **输出路径重复保存**：SaveVideo 节点强制输出到 `Haimi/` 子目录；代理在 ComfyUI 已保存时直接复用文件
- **文件管理添加槽位失败**：修复 "Cannot read properties of null (reading 'path')" 错误
- **服务稳定性**：停止手动启停服务，统一由 systemd 管理

### 技术
- 新增 `/api/presets/videos`、`/api/presets/video/{filename}` 接口
- 工作流配置新增 `filename_prefix` 输入项（默认 `Haimi/MiniMax_H3`）
- proxy `modify_workflow` 强制 SaveVideo 节点输出路径以 `Haimi/` 开头
- proxy `generate` 和 `_mark_completed_and_download` 增加文件复用判断逻辑

## [1.4.0] - 2026-08-27

### 性能优化
- HTTP 连接池（`HTTP_SESSION`）复用 TCP 连接
- SQLite 启用 WAL 模式，提升并发读写性能
- 视频流式传输，避免大文件阻塞内存
- 视频缩略图缓存，减少重复生成
- 动态轮询间隔，根据任务状态自动调整查询频率

## [1.3.0] - 2026-08-26

### 新增
- UI 重构
- 视频缩略图
- 历史管理功能
- 保存路径自定义

## [1.2.0] - 2026-08-25

### 新增
- 文件浏览器
- 预置资源管理
- 连续任务提交
- 配置持久化保存

## [1.1.0] - 2026-08-24

### 新增
- 任务队列
- 进度显示
- 历史记录持久化

## [1.0.0] - 2026-08-23

### 初始版本
- 基础工作流管理
- Web 界面提交视频生成任务
